# AI Build Log Generator Starter

This starter turns structured team notes into the mandatory AI-Native Citizens Markdown build log.

## Use

```bash
node bin/generate-build-log.mjs examples/sample-input.json > build-log.md
```

The CLI validates the required fields and preserves the five key prompts verbatim.

